<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
             x:Class="ProjectWarden.ReviewsPage">
    <ContentPage.Content>
        <StackLayout>
            <forms:AnimationView x:Name="ReviewsSearchAnimation"
                                 Animation="117-progress-bar.json"
                                 AutoPlay="True" 
                                 Loop="true" 
                                 HorizontalOptions="CenterAndExpand"
                                 VerticalOptions="CenterAndExpand"/>
            <ScrollView x:Name="ReviewsScroller" IsVisible="False">
                <ListView x:Name="ReviewsList">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ImageCell
                                ImageSource="{Binding ImageSource}"
                                Text="{Binding Review}"
                                Detail="{Binding ReviewerName}"
                                TextColor="#0074ff">
                            </ImageCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>